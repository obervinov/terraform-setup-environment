---
  name: PR
  
  on:
    push:
      branches:
        - '*'
        - '*/*'
        - '**'
        - '!main'

  jobs:    
    pr:
      uses: obervinov/_templates/.github/workflows/pr.yaml@v4.0.0

    terraform:
      uses: obervinov/_templates/.github/workflows/terraform.yaml@v4.0.0
