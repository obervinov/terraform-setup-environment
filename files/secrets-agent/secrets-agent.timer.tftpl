[Unit]
Description=Poll the secrets proxy for changed secrets

[Timer]
# Runs on boot too, which is what restores secrets after a reboot.
OnBootSec=1min
OnCalendar=${interval}
Persistent=true
RandomizedDelaySec=60s
Unit=secrets-agent.service

[Install]
WantedBy=timers.target
